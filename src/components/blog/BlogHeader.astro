--- 
import Hero from '../Hero.astro';

interface Props {
	title: string;
    description?: string;
}

const {title, description} = Astro.props;

--- 
<header>
        <Hero tagline={description} title={title} align="start" />
</header>